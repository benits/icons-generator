{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Program",
      "type": "node",
      "request": "launch",

      "cwd": "${workspaceFolder}",

      "runtimeArgs": ["-r", "ts-node/register"],
      "args": ["${workspaceFolder}/src/index.ts"],

      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
